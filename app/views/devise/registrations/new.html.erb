<section class="container">
  <div class="row">
  
    <div class="col-sm-6">
      <h3>Fakebookを使うと、友達や同僚、同級生、仲間たちとつながりを深められません。ケータイ、スマートフォンからもアクセスできます。</h3>
      <%= image_tag("pyNVUg5EM0j.png") %>
    </div>
    <div class="col-sm-6">
      <h2 id="title">Sign Up</h2>

      <%= form_for(resource, as: resource_name, url: user_registration_path) do |f| %>
      <%= f.error_messages! %>

      <div class="field col-sm-8">
        <%= f.label :email, class: "sr-only" %>
        <div class="col-sm-10">
          <%= f.email_field :email, autofocus: true, class: "form-control", placeholder: "メールアドレス" %>
        </div>
      </div><br>

      <div class="field col-sm-8">
        <%= f.label :password, class: "sr-only" %>
        <div class="col-sm-10">
          <% if @minimum_password_length %>
            <p><%= @minimum_password_length %>文字以上のパスワードを設定してください</p>
          <% end %>
          <%= f.password_field :password, autocomplete: "off", class: "form-control", placeholder:"パスワード" %>
        </div>
      </div>

      <div class="field col-sm-8">
        <%= f.label :password_confirmation, class: "sr-only" %>
        <div class="col-sm-10">
          <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control", placeholder: "確認用パスワード" %>
        </div>
      </div><br>

      <div class="actions col-sm-8">
        <div class="col-offset-sm-5">
          <%= f.submit "新規登録", class: "btn btn-primary" %>
        </div>
      </div>
    <% end %>
    <br>

    <div class="link col-sm-6 ">
      <%= render "devise/shared/links" %>
    </div>
    <br>

     </div>
  </div>
</section>





