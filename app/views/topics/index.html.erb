p<p id="notice"><%= notice %></p>
<% if user_signed_in? %>
          <li><%= link_to "マイページ", edit_user_registration_path %></li>
          <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "ログイン", new_user_session_path %></li>
          <li><%= link_to "サインアップ", new_user_registration_path %></li>
        <% end %>
<h1>Listing Topics</h1>

<% @topics.each do |t| %>
  <p>
  <%= t.user.name %></p>
        <p><%= link_to 'Show', topic_path(t.id) %>
        <%= link_to 'Edit', edit_topic_path(t.id) %>
        <%= link_to 'Destroy', topic_path(t.id), method: :delete, data: { confirm: 'Are you sure?' } %></p>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Topic', new_topic_path %>
